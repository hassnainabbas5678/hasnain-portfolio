# Optional override for the deployed contact endpoint. The default is /api/contact.
VITE_CONTACT_ENDPOINT=

# Configure these in Netlify (never expose RESEND_API_KEY with a VITE_ prefix).
RESEND_API_KEY=re_your_resend_api_key
CONTACT_FROM_EMAIL=Portfolio <contact@your-verified-domain.com>
CONTACT_RECIPIENT=hassnainlilani@gmail.com
